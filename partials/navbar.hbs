<nav class="navbar navbar-expand-lg navbar-dark w-100">
    <div class="{{#is "home"}}container-fluid{{else}}container{{/is}}">
        {{#if @site.logo}}
            <a class="navbar-brand" href="{{@site.url}}"><img src="{{@site.logo}}" alt="{{@site.title}}"/></a>
        {{else}}
            <a class="navbar-brand" href="{{@site.url}}">{{@site.title}}</a>
        {{/if}}
        {{#if @site.navigation}}
            {{navigation}}
        {{/if}}

        {{^is "home"}}
            <div class="ml-auto">
                <a class="btn btn-primary btn-lg" href="/">Find Out More</a>
            </div>
        {{/is}}
    </div>
</nav>
