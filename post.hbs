{{!< default}}

<header>
    {{> navbar }}
</header>
{{#post}}
    <main id="site-main" class="site-main">
        <div class="post-full {{post_class}} {{#unless feature_image}}no-image{{/unless}}">
            <header class="container post-full-header">
                {{#if primary_tag}}
                    <div class="post-full-tags">
                        {{#primary_tag}}
                            <a href="{{url}}">{{name}}</a>
                        {{/primary_tag}}
                    </div>
                {{/if}}

                <h1 class="post-full-title">{{title}}</h1>
                {{#if custom_excerpt}}
                    <p class="post-full-custom-excerpt">{{custom_excerpt}}</p>
                {{/if}}

                {{> author-list }}
            </header>


            {{#if feature_image}}
                <section class="container">
                    <figure class="post-full-image">
                        {{!-- This is a responsive image, it loads different sizes depending on device
                        https://medium.freecodecamp.org/a-guide-to-responsive-images-with-ready-to-use-templates-c400bd65c433 --}}
                        <img
                                srcset="{{img_url feature_image size="s"}} 300w,
                            {{img_url feature_image size="m"}} 600w,
                            {{img_url feature_image size="l"}} 1000w,
                            {{img_url feature_image size="xl"}} 2000w"
                                sizes="(max-width: 800px) 400px,
                        (max-width: 1170px) 1170px,
                            2000px"
                                src="{{img_url feature_image size="xl"}}"
                                alt="{{title}}"
                        />
                    </figure>
                </section>
            {{/if}}

            <section class="container post-full-content">
                {{content}}
            </section>
        </div>
    </main>
{{/post}}
